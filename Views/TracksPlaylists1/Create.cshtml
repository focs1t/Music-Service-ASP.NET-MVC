@model CourseWork.Models.TracksPlaylists

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>TracksPlaylists</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="tracksId" class="control-label"></label>
                @*<input asp-for="tracksId" class="form-control" />
                <span asp-validation-for="tracksId" class="text-danger"></span>*@
                <select asp-for="tracksId" class="form-control" asp-items="ViewBag.tracksId"></select>
                @*@foreach (var item in ViewBag.tracksId)
                {
                    <div>
                        <input type="checkbox" name="tracksId" value="@item.Value" /> @item.Text
                    </div>
                }*@
            </div>
            <div class="form-group">
                <label asp-for="playlistsId" class="control-label"></label>
                <select asp-for="playlistsId" class ="form-control" asp-items="ViewBag.playlistsId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
