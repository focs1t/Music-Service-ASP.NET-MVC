@model CourseWork.Models.TracksPlaylists

<h1>Удаление</h1>

<div class="container">
    <h3>Вы уверены, что хотите удалить этот файл?</h3>
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Плейлисты и треки номер @Model?.Id</h4>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">Трек</dt>
                <dd class="col-sm-10">@string.Format("{0} - {1}", Model.tracks.name, Model.tracks.artists.name)</dd>
                <dt class="col-sm-2">Плейлист</dt>
                <dd class="col-sm-10">@string.Format("{0} - {1}", Model.playlists.name, Model.playlists.username)</dd>
            </dl>
        </div>
    </div>
    <form asp-action="Delete" class="mt-3">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Удалить</button>
        <a asp-action="Index" class="btn btn-secondary">Вернуться к списку</a>
    </form>
</div>
